# Tally Web

This directory is the independent frontend project for Tally. It will contain
the Next.js application implemented from `../docs/frontend-implementation-plan.md`.

It intentionally has no workspace dependency on `../api`.
