# Released API contract

`openapi.json` is the pinned OpenAPI 3.1 artifact consumed by the frontend.
Update it only when the API publishes a new released contract, then update
`openapi.lock.json` with the artifact hash.
